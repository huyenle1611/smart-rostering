spring:
  application:
    name: rostering

  datasource:
    url: jdbc:mysql://localhost:3306/rostering
    username: root
    password: rootpw

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
